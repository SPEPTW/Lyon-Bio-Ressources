{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Type_categorie</th>
                <td>{{ categorie.titre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('lbr_categorie_index') }}"><span class="text-white"><button type="btn" class="btn btn-primary">Retour
                à la liste</span></button></a>

    <a href="{{ path('lbr_categorie_edit', {'id': categorie.id}) }}"><button type="btn" class="btn btn-warning"><span class="text-white">Editer</span></button></a>
    <hr>
    
    {{ include('lbr/categorie/_delete_form.html.twig') }}

    
{% endblock %}
