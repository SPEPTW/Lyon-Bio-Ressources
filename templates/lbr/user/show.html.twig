{% extends 'base.html.twig' %}

{% block title %}Utilisateur{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="container">
        <div class="row">
            <div class="col-12 flex-column mt-5">

                <h1>Utilisateur</h1>

            </div>
        </div>
    </div>
</div>
                <div class="col-6 .d-inline flex">
    <table class="table">
        <tbody>               
                    <tr>
                        <th>Email</th>
                        <td>{{ user.email }}</td>
                    </tr>
            </div>
            </tr>

           
           
        </tbody>
    </table>
        </div>
           
   
    <a href="{{ path('lbr_user_index') }}"><span class="text-white"><button type="btn" class="btn btn-primary">Retour à la liste</span></button></a>

    <a href="{{ path('lbr_user_edit', {'id': user.id}) }}"><button type="btn" class="btn btn-warning"><span class="text-white">Editer</span></button></a>

    {{ include('lbr/user/_delete_form.html.twig') }}
{% endblock %}
