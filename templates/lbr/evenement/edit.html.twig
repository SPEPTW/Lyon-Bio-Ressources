{% extends 'base.html.twig' %}

{% block title %}Editer un Evenement{% endblock %}

{% block body %}
     
<div class="container-fluid">
    <div class="container">
        <div class="row">
            <div class="col-12 flex-column mt-5">
            <h1>Editer un Evenement</h1>       

    {{ include('lbr/evenement/_form.html.twig', {'button_label': 'Sauvegarder'}) }}
               

    <button type="button" class="btn btn-primary"><a href="{{ path('lbr_evenement_index') }}"><span class="text-white">Retour à la liste</a></button>

    
    {{ include('lbr/evenement/_delete_form.html.twig') }}

             </div>
        </div>
    </div>
</div>


{% endblock %}
